apiVersion: v1
kind: Service
metadata:
  name: {{ include "komodor-security-reporter.fullname" . }}
  labels:
    {{- include "komodor-security-reporter.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: metrics
      protocol: TCP
      name: metrics
  selector:
    {{- include "komodor-security-reporter.selectorLabels" . | nindent 4 }}
